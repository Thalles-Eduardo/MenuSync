# Hero Interativo da Home — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir a home do site do restaurante MenuSync como uma tela única de hero interativo (navbar, hero text, prato central, painel glass de comentários/ingredientes e carrossel de pratos clicável), com animações GSAP.

**Architecture:** `app/(site)/page.tsx` (server component) importa os dados hardcoded e renderiza uma ilha client `HeroShowcase`, que detém o estado (prato ativo + aba ativa) e orquestra componentes de apresentação puros. Dados dos 4 pratos ficam num módulo estático (sem banco). Animações via GSAP/`useGSAP`.

**Tech Stack:** Next.js 16 (App Router), React 19, TypeScript, Tailwind CSS v4, GSAP + @gsap/react.

**Nota sobre verificação:** Conforme o spec aprovado e o roadmap (testes = Fase 12), este plano não introduz runner de testes. Cada tarefa termina num estado rodável, verificado por `npm run lint`, `npm run build` e checagem visual em `npm run dev`. Referência de design: `docs/superpowers/specs/2026-07-06-hero-home-design.md` e `esbocoHero.png`.

---

## File Structure

```
app/(site)/
├── page.tsx                 # server: importa dishes, renderiza <HeroShowcase dishes={dishes} />
├── _data/
│   └── dishes.ts            # tipos Review/Dish + array dishes (4 pratos)
└── _components/
    ├── HeroShowcase.tsx     # "use client": estado + GSAP; orquestra os filhos
    ├── Navbar.tsx           # ícones buscar · menu · carrinho
    ├── HeroText.tsx         # tagline, título, preço, botões
    ├── DishPlate.tsx        # imagem do prato central
    ├── ReviewsPanel.tsx     # painel glass: abas + cards / ingredientes
    └── DishCarousel.tsx     # miniaturas clicáveis
```

Também modifica: `app/page.tsx` (já delega para `(site)/page`), `app/globals.css` (limpeza), `app/layout.tsx` (`lang`), `package.json` (deps GSAP).

---

## Task 1: Módulo de dados dos pratos

**Files:**
- Create: `app/(site)/_data/dishes.ts`

- [ ] **Step 1: Criar o módulo de dados**

Create `app/(site)/_data/dishes.ts`:

```ts
export type Review = {
  author: string;
  rating: number; // 1-5
  text: string;
};

export type Dish = {
  id: string;
  name: string;
  tagline: string;
  price: number;
  plate: string; // imagem central (alta)
  thumb: string; // miniatura do carrossel
  reviews: Review[];
  ingredients: string[];
};

export const dishes: Dish[] = [
  {
    id: "teishoku",
    name: "Teishoku Japonês",
    tagline: "#1 Melhor prato da casa!",
    price: 149.99,
    plate: "/plate1.webp",
    thumb: "/food1.webp",
    reviews: [
      { author: "Maria Bretanio", rating: 3, text: "Combinação equilibrada, veio tudo fresquinho." },
      { author: "Barry Alien", rating: 3, text: "Porção generosa, o frango teriyaki é o destaque." },
      { author: "Bruce Banner", rating: 3, text: "Prato completo, saí muito satisfeito." },
      { author: "Carla Almeida", rating: 3, text: "Apresentação linda e sabor autêntico." },
    ],
    ingredients: ["Salmão", "Arroz japonês", "Frango teriyaki", "Tempurá de camarão", "Sunomono", "Shoyu"],
  },
  {
    id: "sopa-misso",
    name: "Sopa de Missô",
    tagline: "Reconforto em cada colher",
    price: 24.9,
    plate: "/plate3.png",
    thumb: "/food3.webp",
    reviews: [
      { author: "Rafael Souza", rating: 3, text: "Caldo no ponto certo de sal, bem tradicional." },
      { author: "Ana Lima", rating: 3, text: "Tofu macio e alga fresca, adorei." },
      { author: "Pedro Nunes", rating: 3, text: "Perfeita para começar a refeição." },
      { author: "Júlia Reis", rating: 3, text: "Aconchegante e leve." },
    ],
    ingredients: ["Pasta de missô", "Tofu", "Alga wakame", "Cebolinha", "Dashi"],
  },
  {
    id: "sushi-10",
    name: "Sushi 10 P/c",
    tagline: "Seleção do sushiman",
    price: 89.9,
    plate: "/plate4.png",
    thumb: "/food4.webp",
    reviews: [
      { author: "Lucas Prado", rating: 3, text: "Peixe fresco e corte impecável." },
      { author: "Marina Costa", rating: 3, text: "Variedade ótima para dividir." },
      { author: "Diego Alves", rating: 3, text: "Arroz temperado na medida certa." },
      { author: "Bianca Melo", rating: 3, text: "Melhor combinado da região." },
    ],
    ingredients: ["Salmão", "Atum", "Camarão", "Arroz shari", "Nori", "Gengibre", "Wasabi"],
  },
  {
    id: "okonomiyaki",
    name: "Okonomiyaki",
    tagline: "A pizza japonesa da casa",
    price: 54.9,
    plate: "/plate2.png",
    thumb: "/food2.webp",
    reviews: [
      { author: "Thiago Ramos", rating: 3, text: "Cheio de sabor, o katsuobushi faz diferença." },
      { author: "Sofia Dias", rating: 3, text: "Massa fofinha e bem recheada." },
      { author: "Gabriel Pinto", rating: 3, text: "Molho na medida, viciante." },
      { author: "Helena Cruz", rating: 3, text: "Diferente e delicioso." },
    ],
    ingredients: ["Repolho", "Massa de okonomiyaki", "Molho okonomiyaki", "Maionese japonesa", "Katsuobushi", "Aonori"],
  },
];
```

- [ ] **Step 2: Verificar tipos**

Run: `npx tsc --noEmit`
Expected: sem erros.

- [ ] **Step 3: Commit**

```bash
git add "app/(site)/_data/dishes.ts"
git commit -m "feat(hero): dados hardcoded dos 4 pratos"
```

---

## Task 2: Instalar dependências GSAP

**Files:**
- Modify: `package.json`

- [ ] **Step 1: Instalar gsap e @gsap/react**

Run: `npm install gsap @gsap/react`
Expected: instala sem erro; `package.json` passa a listar `gsap` e `@gsap/react` em dependencies.

- [ ] **Step 2: Verificar build ainda funciona**

Run: `npm run build`
Expected: build conclui sem erros (a home ainda está vazia).

- [ ] **Step 3: Commit**

```bash
git add package.json package-lock.json
git commit -m "chore: adiciona gsap e @gsap/react"
```

---

## Task 3: Shell do HeroShowcase + página server (fundo)

Objetivo: página renderiza o fundo `bgHero.webp` em tela cheia e a ilha client vazia com estado. Estado ainda sem UI.

**Files:**
- Create: `app/(site)/_components/HeroShowcase.tsx`
- Modify: `app/(site)/page.tsx`
- Modify: `app/globals.css` (remove `.hero` obsoleta)

- [ ] **Step 1: Criar o shell do HeroShowcase**

Create `app/(site)/_components/HeroShowcase.tsx`:

```tsx
"use client";

import { useState } from "react";
import type { Dish } from "../_data/dishes";

type Tab = "reviews" | "ingredients";

export default function HeroShowcase({ dishes }: { dishes: Dish[] }) {
  const [activeDishId, setActiveDishId] = useState(dishes[0]?.id ?? "");
  const [activeTab, setActiveTab] = useState<Tab>("reviews");

  const activeDish = dishes.find((d) => d.id === activeDishId) ?? dishes[0];

  function handleSelect(id: string) {
    setActiveDishId(id);
    setActiveTab("reviews");
  }

  return (
    <section
      className="relative min-h-screen w-full overflow-hidden bg-cover bg-center text-white"
      style={{ backgroundImage: "url('/bgHero.webp')" }}
      aria-label={`Prato em destaque: ${activeDish?.name}`}
    >
      {/* Navbar, Hero, Prato, Painel e Carrossel entram nas próximas tarefas.
          Referências temporárias para não quebrar o lint enquanto o estado
          ainda não é usado na UI: */}
      <span className="sr-only">{activeDish?.name}</span>
      <button className="sr-only" onClick={() => handleSelect(activeDish.id)}>select</button>
      <span className="sr-only">{activeTab}</span>
      <button className="sr-only" onClick={() => setActiveTab("ingredients")}>tab</button>
    </section>
  );
}
```

- [ ] **Step 2: Ligar a página server à ilha client**

Replace the contents of `app/(site)/page.tsx` with:

```tsx
import HeroShowcase from "./_components/HeroShowcase";
import { dishes } from "./_data/dishes";

export default function SiteHome() {
  return <HeroShowcase dishes={dishes} />;
}
```

- [ ] **Step 3: Remover a regra `.hero` obsoleta do globals.css**

In `app/globals.css`, delete the trailing block:

```css
.hero {
  width: 100vw;
  height: 100vh;
  background: url("/bgHero.webp") ;
  
}
```

(Deixe o restante do arquivo intacto.)

- [ ] **Step 4: Verificar lint e visual**

Run: `npm run lint`
Expected: sem erros.

Run: `npm run dev` e abrir `http://localhost:3000`.
Expected: a tela mostra o fundo do restaurante japonês (`bgHero.webp`) ocupando a viewport inteira, sem conteúdo por cima ainda.

- [ ] **Step 5: Commit**

```bash
git add "app/(site)/_components/HeroShowcase.tsx" "app/(site)/page.tsx" app/globals.css
git commit -m "feat(hero): shell do HeroShowcase com fundo em tela cheia"
```

---

## Task 4: Navbar

**Files:**
- Create: `app/(site)/_components/Navbar.tsx`
- Modify: `app/(site)/_components/HeroShowcase.tsx`

- [ ] **Step 1: Criar a Navbar**

Create `app/(site)/_components/Navbar.tsx`:

```tsx
import Image from "next/image";

const icons = [
  { src: "/icons/search.svg", alt: "Buscar" },
  { src: "/icons/menu.svg", alt: "Menu" },
  { src: "/icons/cart.svg", alt: "Carrinho" },
];

export default function Navbar() {
  return (
    <nav className="flex items-center justify-end gap-6 px-8 py-6 md:px-12">
      {icons.map((icon) => (
        <button
          key={icon.alt}
          aria-label={icon.alt}
          className="opacity-90 transition hover:scale-110 hover:opacity-100"
        >
          <Image src={icon.src} alt={icon.alt} width={26} height={26} />
        </button>
      ))}
    </nav>
  );
}
```

- [ ] **Step 2: Integrar no HeroShowcase**

In `app/(site)/_components/HeroShowcase.tsx`, add the import at the top:

```tsx
import Navbar from "./Navbar";
```

Then, inside the `<section>`, replace the temporary `sr-only` navbar placeholder line (the first `<span className="sr-only">{activeDish?.name}</span>`) — keep the other temporary refs for now — by adding `<Navbar />` as the first child:

```tsx
    >
      <Navbar />
      <span className="sr-only">{activeDish?.name}</span>
      <button className="sr-only" onClick={() => handleSelect(activeDish.id)}>select</button>
      <span className="sr-only">{activeTab}</span>
      <button className="sr-only" onClick={() => setActiveTab("ingredients")}>tab</button>
```

- [ ] **Step 3: Verificar visual**

Run: `npm run dev` e abrir `http://localhost:3000`.
Expected: no topo direito aparecem os 3 ícones (buscar, menu, carrinho); hover aumenta levemente cada um.

- [ ] **Step 4: Commit**

```bash
git add "app/(site)/_components/Navbar.tsx" "app/(site)/_components/HeroShowcase.tsx"
git commit -m "feat(hero): navbar com ícones buscar/menu/carrinho"
```

---

## Task 5: HeroText (tagline, título, preço, botões)

**Files:**
- Create: `app/(site)/_components/HeroText.tsx`
- Modify: `app/(site)/_components/HeroShowcase.tsx`

- [ ] **Step 1: Criar o HeroText**

Create `app/(site)/_components/HeroText.tsx`:

```tsx
import Image from "next/image";
import type { Dish } from "../_data/dishes";

const brl = new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL" });

export default function HeroText({ dish }: { dish: Dish }) {
  return (
    <div className="hero-text max-w-md">
      <p className="mb-3 text-sm font-semibold tracking-wide text-yellow">{dish.tagline}</p>

      <h1
        className="text-6xl leading-[0.95] font-bold md:text-7xl"
        style={{ fontFamily: "var(--font-ming), var(--font-eczar), serif" }}
      >
        {dish.name}
      </h1>

      <p className="mt-6 text-2xl font-medium">{brl.format(dish.price)}</p>

      <div className="mt-10 flex items-center gap-8">
        <button className="flex items-center gap-2 text-white/90 transition hover:text-white">
          <Image src="/icons/play.svg" alt="" width={28} height={28} />
          <span>Play no vídeo</span>
        </button>
        <button className="flex items-center gap-2 rounded-md bg-salmon px-5 py-3 font-semibold text-white transition hover:brightness-110">
          <Image src="/icons/cart.svg" alt="" width={20} height={20} />
          <span>Adicionar</span>
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Integrar no HeroShowcase com layout de grid**

In `app/(site)/_components/HeroShowcase.tsx`, add import:

```tsx
import HeroText from "./HeroText";
```

Replace the block of temporary `sr-only` refs (the `<span>{activeDish?.name}</span>` through the tab `<button>`) with a content grid that starts holding the hero text. The `<section>` body should now be:

```tsx
      <Navbar />

      <div className="relative z-10 grid grid-cols-1 items-center gap-8 px-8 pt-4 pb-40 md:px-12 lg:grid-cols-[1fr_1.1fr_1fr]">
        <HeroText dish={activeDish} />

        {/* Prato central (Task 6) e Painel (Task 8) entram aqui */}
        <div />
        <div>
          {/* placeholder temporário do painel — remove na Task 8 */}
          <button className="sr-only" onClick={() => setActiveTab("ingredients")}>{activeTab}</button>
        </div>
      </div>

      {/* Carrossel (Task 7) entra aqui */}
      <button className="sr-only" onClick={() => handleSelect(activeDish.id)}>select</button>
```

- [ ] **Step 3: Verificar visual**

Run: `npm run dev`.
Expected: à esquerda aparecem tagline (amarela), título grande "Teishoku Japonês" em fonte display sobre o pergaminho do fundo, preço "R$ 149,99" e os botões "Play no vídeo" e "Adicionar" (salmão).

- [ ] **Step 4: Commit**

```bash
git add "app/(site)/_components/HeroText.tsx" "app/(site)/_components/HeroShowcase.tsx"
git commit -m "feat(hero): bloco de texto (tagline, título, preço, CTAs)"
```

---

## Task 6: DishPlate (prato central)

**Files:**
- Create: `app/(site)/_components/DishPlate.tsx`
- Modify: `app/(site)/_components/HeroShowcase.tsx`

- [ ] **Step 1: Criar o DishPlate**

Create `app/(site)/_components/DishPlate.tsx`:

```tsx
import Image from "next/image";
import type { Dish } from "../_data/dishes";

export default function DishPlate({ dish }: { dish: Dish }) {
  return (
    <div className="flex justify-center">
      <Image
        key={dish.id}
        src={dish.plate}
        alt={dish.name}
        width={520}
        height={520}
        priority
        className="plate-img h-auto w-[70vw] max-w-[520px] drop-shadow-2xl"
      />
    </div>
  );
}
```

- [ ] **Step 2: Integrar no HeroShowcase**

In `app/(site)/_components/HeroShowcase.tsx`, add import:

```tsx
import DishPlate from "./DishPlate";
```

Replace the empty center placeholder `<div />` (middle grid column) with:

```tsx
        <DishPlate dish={activeDish} />
```

- [ ] **Step 3: Verificar visual**

Run: `npm run dev`.
Expected: o prato Teishoku (`plate1.webp`) aparece grande no centro, com sombra, sobre o emblema do fundo.

- [ ] **Step 4: Commit**

```bash
git add "app/(site)/_components/DishPlate.tsx" "app/(site)/_components/HeroShowcase.tsx"
git commit -m "feat(hero): prato central"
```

---

## Task 7: DishCarousel (miniaturas clicáveis) — interatividade principal

Ao fim desta tarefa, clicar numa miniatura troca prato central, título e preço.

**Files:**
- Create: `app/(site)/_components/DishCarousel.tsx`
- Modify: `app/(site)/_components/HeroShowcase.tsx`

- [ ] **Step 1: Criar o DishCarousel**

Create `app/(site)/_components/DishCarousel.tsx`:

```tsx
import Image from "next/image";
import type { Dish } from "../_data/dishes";

type Props = {
  dishes: Dish[];
  activeId: string;
  onSelect: (id: string) => void;
};

export default function DishCarousel({ dishes, activeId, onSelect }: Props) {
  return (
    <ul className="flex items-start justify-center gap-8">
      {dishes.map((dish) => {
        const isActive = dish.id === activeId;
        return (
          <li key={dish.id} className="dish-thumb w-28 text-center">
            <button
              onClick={() => onSelect(dish.id)}
              aria-pressed={isActive}
              className="group flex flex-col items-center gap-2"
            >
              <span
                className={`overflow-hidden rounded-full border-2 transition duration-300 group-hover:scale-105 ${
                  isActive ? "border-salmon" : "border-white/40"
                }`}
              >
                <Image src={dish.thumb} alt={dish.name} width={80} height={80} className="h-20 w-20 object-cover" />
              </span>
              <span className={`text-sm ${isActive ? "text-white" : "text-white/70"}`}>{dish.name}</span>
            </button>
          </li>
        );
      })}
    </ul>
  );
}
```

- [ ] **Step 2: Integrar no HeroShowcase**

In `app/(site)/_components/HeroShowcase.tsx`, add import:

```tsx
import DishCarousel from "./DishCarousel";
```

Replace the temporary carousel placeholder line (`<button className="sr-only" onClick={() => handleSelect(activeDish.id)}>select</button>`) with an absolutely-positioned carousel near the bottom:

```tsx
      <div className="absolute bottom-10 left-0 right-0 z-10 lg:right-[32%]">
        <DishCarousel dishes={dishes} activeId={activeDish.id} onSelect={handleSelect} />
      </div>
```

- [ ] **Step 3: Verificar interatividade**

Run: `npm run dev`.
Expected: na base aparecem as 4 miniaturas com nomes; a ativa tem borda salmão. Clicar em "Sopa de Missô", "Sushi 10 P/c" e "Okonomiyaki" troca o prato central, o título e o preço à esquerda. Hover aumenta levemente a miniatura.

- [ ] **Step 4: Commit**

```bash
git add "app/(site)/_components/DishCarousel.tsx" "app/(site)/_components/HeroShowcase.tsx"
git commit -m "feat(hero): carrossel de pratos clicável (troca prato ativo)"
```

---

## Task 8: ReviewsPanel (painel glass com abas)

**Files:**
- Create: `app/(site)/_components/ReviewsPanel.tsx`
- Modify: `app/(site)/_components/HeroShowcase.tsx`

- [ ] **Step 1: Criar o ReviewsPanel**

Create `app/(site)/_components/ReviewsPanel.tsx`:

```tsx
import type { Dish } from "../_data/dishes";

type Tab = "reviews" | "ingredients";

type Props = {
  dish: Dish;
  activeTab: Tab;
  onTabChange: (tab: Tab) => void;
};

function Stars({ rating }: { rating: number }) {
  return (
    <span className="text-yellow" aria-label={`${rating} de 5 estrelas`}>
      {"★".repeat(rating)}
      <span className="text-white/30">{"★".repeat(5 - rating)}</span>
    </span>
  );
}

export default function ReviewsPanel({ dish, activeTab, onTabChange }: Props) {
  return (
    <div className="rounded-3xl border border-white/20 bg-white/10 p-5 backdrop-blur-md">
      <div className="mb-4 flex gap-6 border-b border-white/15 pb-3">
        <button
          onClick={() => onTabChange("reviews")}
          className={`text-sm font-semibold transition ${activeTab === "reviews" ? "text-white" : "text-white/50"}`}
        >
          Comentários
        </button>
        <button
          onClick={() => onTabChange("ingredients")}
          className={`text-sm font-semibold transition ${activeTab === "ingredients" ? "text-white" : "text-white/50"}`}
        >
          Ingredientes
        </button>
      </div>

      {activeTab === "reviews" ? (
        <ul className="flex max-h-[420px] flex-col gap-3 overflow-y-auto pr-1">
          {dish.reviews.map((review) => (
            <li key={review.author} className="review-card rounded-2xl bg-white/90 p-4 text-neutral-800">
              <div className="mb-2 flex items-center gap-3">
                <span className="h-9 w-9 shrink-0 rounded-full bg-neutral-900" />
                <span className="flex-1 text-sm font-bold">{review.author}</span>
                <Stars rating={review.rating} />
              </div>
              <p className="text-xs leading-relaxed text-neutral-600">{review.text}</p>
            </li>
          ))}
        </ul>
      ) : (
        <ul className="flex flex-col gap-2">
          {dish.ingredients.map((ingredient) => (
            <li key={ingredient} className="review-card rounded-xl bg-white/85 px-4 py-3 text-sm font-medium text-neutral-800">
              {ingredient}
            </li>
          ))}
        </ul>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Integrar no HeroShowcase**

In `app/(site)/_components/HeroShowcase.tsx`, add import:

```tsx
import ReviewsPanel from "./ReviewsPanel";
```

Replace the right-column placeholder `<div>` (the one containing the temporary tab `<button>`) with:

```tsx
        <ReviewsPanel dish={activeDish} activeTab={activeTab} onTabChange={setActiveTab} />
```

- [ ] **Step 3: Verificar visual e interatividade**

Run: `npm run dev`.
Expected: à direita aparece o painel glass (fundo translúcido, blur, borda clara) com abas "Comentários"/"Ingredientes"; a aba Comentários mostra 4 cards (avatar, nome, ★★★, texto); clicar em "Ingredientes" troca para a lista de ingredientes do prato ativo; trocar de prato no carrossel volta a aba para Comentários e atualiza o conteúdo.

- [ ] **Step 4: Commit**

```bash
git add "app/(site)/_components/ReviewsPanel.tsx" "app/(site)/_components/HeroShowcase.tsx"
git commit -m "feat(hero): painel glass com abas comentários/ingredientes"
```

---

## Task 9: Animações GSAP

Adiciona timeline de entrada, animação de troca de prato e stagger na troca de aba, com `useGSAP`. Respeita `prefers-reduced-motion`.

**Files:**
- Modify: `app/(site)/_components/HeroShowcase.tsx`

- [ ] **Step 1: Registrar useGSAP e criar as animações**

In `app/(site)/_components/HeroShowcase.tsx`, add imports at the top (below the existing `useState` import):

```tsx
import { useRef } from "react";
import gsap from "gsap";
import { useGSAP } from "@gsap/react";
```

Inside the component, before `return`, add a scope ref and three `useGSAP` blocks:

```tsx
  const scope = useRef<HTMLElement>(null);
  const reduce = typeof window !== "undefined" && window.matchMedia("(prefers-reduced-motion: reduce)").matches;

  // Entrada (uma vez, no mount)
  useGSAP(
    () => {
      if (reduce) return;
      const tl = gsap.timeline({ defaults: { ease: "power3.out", duration: 0.7 } });
      tl.from(".hero-text", { x: -40, opacity: 0 })
        .from(".plate-img", { scale: 0.8, rotate: -8, opacity: 0 }, "-=0.4")
        .from(".dish-thumb", { y: 30, opacity: 0, stagger: 0.08 }, "-=0.3")
        .from(".review-card", { y: 20, opacity: 0, stagger: 0.06 }, "-=0.5");
    },
    { scope }
  );

  // Troca de prato
  useGSAP(
    () => {
      if (reduce) return;
      gsap.fromTo(".plate-img", { scale: 0.9, rotate: -6, opacity: 0.3 }, { scale: 1, rotate: 0, opacity: 1, duration: 0.5, ease: "power2.out" });
      gsap.fromTo(".hero-text", { x: -20, opacity: 0.4 }, { x: 0, opacity: 1, duration: 0.4, ease: "power2.out" });
    },
    { scope, dependencies: [activeDishId] }
  );

  // Troca de aba (stagger nos cards)
  useGSAP(
    () => {
      if (reduce) return;
      gsap.from(".review-card", { y: 12, opacity: 0, stagger: 0.05, duration: 0.35, ease: "power2.out" });
    },
    { scope, dependencies: [activeTab] }
  );
```

- [ ] **Step 2: Ligar o ref ao `<section>`**

In the same file, add the ref to the root element:

```tsx
    <section
      ref={scope}
      className="relative min-h-screen w-full overflow-hidden bg-cover bg-center text-white"
```

- [ ] **Step 3: Verificar animações**

Run: `npm run dev`.
Expected: ao carregar, o hero text desliza da esquerda, o prato central escala/rotaciona ao aparecer e as miniaturas/cards entram em sequência. Ao clicar numa miniatura, o prato central faz um leve cross-fade/rotação e o texto reanima. Ao trocar de aba, os cards entram em stagger. Com "reduzir movimento" ativo no SO, não há animação.

- [ ] **Step 4: Verificar lint/build**

Run: `npm run lint && npm run build`
Expected: sem erros.

- [ ] **Step 5: Commit**

```bash
git add "app/(site)/_components/HeroShowcase.tsx"
git commit -m "feat(hero): animações GSAP (entrada, troca de prato e de aba)"
```

---

## Task 10: Responsividade + limpezas finais

**Files:**
- Modify: `app/(site)/_components/HeroShowcase.tsx`
- Modify: `app/layout.tsx`

- [ ] **Step 1: Ajustar `lang` do layout**

In `app/layout.tsx`, change `lang="en"` to `lang="pt-BR"` on the `<html>` element.

- [ ] **Step 2: Garantir empilhamento no mobile**

In `app/(site)/_components/HeroShowcase.tsx`, confirm the content grid uses `grid-cols-1` por padrão e `lg:grid-cols-[1fr_1.1fr_1fr]` (já definido na Task 5). Ajustar o wrapper do carrossel para não sobrepor no mobile trocando a classe do container do carrossel para:

```tsx
      <div className="static mt-8 px-8 pb-10 lg:absolute lg:bottom-10 lg:left-0 lg:right-[32%] lg:mt-0 lg:px-0">
        <DishCarousel dishes={dishes} activeId={activeDish.id} onSelect={handleSelect} />
      </div>
```

E garantir que a `<section>` permita rolagem no mobile trocando `overflow-hidden` por `overflow-x-hidden`:

```tsx
      className="relative min-h-screen w-full overflow-x-hidden bg-cover bg-center text-white"
```

- [ ] **Step 3: Verificar responsivo**

Run: `npm run dev`, abrir o devtools em modo mobile (~390px).
Expected: as regiões empilham na ordem hero text → prato → painel → carrossel, tudo legível e sem sobreposição; no desktop (≥1024px) o layout de 3 colunas do esboço se mantém.

- [ ] **Step 4: Verificação final**

Run: `npm run lint && npm run build`
Expected: ambos sem erros.

Checklist manual (desktop):
- Layout bate com `esbocoHero.png`.
- Clicar em cada uma das 4 miniaturas troca prato/título/preço/painel.
- Abas alternam comentários/ingredientes; trocar prato reseta para Comentários.
- Animações de entrada, troca de prato e troca de aba ocorrem.

- [ ] **Step 5: Commit**

```bash
git add "app/(site)/_components/HeroShowcase.tsx" app/layout.tsx
git commit -m "feat(hero): responsividade mobile e ajuste de lang"
```

---

## Self-Review (cobertura do spec)

- Navbar (§2.1) → Task 4 ✅
- Hero text/tagline/título/preço/botões (§2.2) → Task 5 ✅
- Prato central (§2.3) → Task 6 ✅
- Painel glass + abas comentários/ingredientes (§2.4) → Task 8 ✅
- Carrossel clicável (§2.5) → Task 7 ✅
- Arquitetura server page + ilha client (§3) → Tasks 1, 3 ✅
- Modelo de dados dos 4 pratos (§4) → Task 1 ✅
- Estado + reset de aba ao trocar prato (§5) → Tasks 3, 7, 8 ✅
- Estilo glass/paleta/fontes + limpeza globals/lang (§6) → Tasks 3, 5, 8, 10 ✅
- Animações GSAP + reduced-motion (§7) → Task 9 ✅
- Responsividade (§8) → Task 10 ✅
- Verificação build/lint/manual (§9) → cada Task + Task 10 ✅
- Pendências (§10) → refletidas nos dados fictícios e placeholders (sem ação) ✅
